import styled from '@emotion/styled';
import TableCell from '@material-ui/core/TableCell';

export const CheckColumn = styled(TableCell)({
  width: '10%',
});
export const NameColumn = styled(TableCell)({
  width: '30%',
});
export const DomainColumn = styled(TableCell)({
  width: '25%',
});
export const UnitColumn = styled(TableCell)({
  width: '30%',
});
export const AlertColumn = styled(TableCell)({
  width: '5%',
});